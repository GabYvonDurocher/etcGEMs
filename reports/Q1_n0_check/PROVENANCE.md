<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/Q1_n0_check/`

**STATUS: CURRENT** — a diagnosis, not an experiment: no solves, no fits, no data altered and no conversion changed. Arithmetic on committed tables only -- the respirometry derived tables and E4's committed fit_predictions.csv. Its headline is a NULL result on the question asked, and it contradicts OPEN_ITEMS 1.9's framing rather than confirming it: the respiration likelihood's observable contains N0 but not cell_carbon_fg, so "respiration R2 is at risk" is wrong, while CUE moves materially and changes shape. 1.9 remains Parsa's to resolve.

*Evidence:* `python3 reports/Q1_n0_check/task1_residuals.py` (72 residuals, the marginal regressions and the paired-media test, seconds); `task1_figure.py`; `task2_sensitivity.py` -- which first reproduces all seven derived columns from the table's own constants to <=8.5e-14 and asserts it, so the TASK 0 chain is re-checked on every run, then recomputes under config.R's constants; `task3_size_growth.py` (power against the Schaechter band). The biomass-integral identity N0*(exp(r*T_end)-1)/r holds to 6.9e-14 on R2A/LB (n=117) and 7.1e-14 on M9 (n=66).

**This report was last written at** `7d85064` (2026-09-11) — Q1: size-growth magnitude -- the paired test was powered, and the artefact is the wrong shape

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
