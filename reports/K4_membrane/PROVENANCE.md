<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/K4_membrane/`

**STATUS: CURRENT** — every number in the report was produced by the three scripts beside it, on the code and the strain models as they stand in this branch, and all three re-run clean.

*Evidence:* `python3 reports/K4_membrane/task1_etc_complement.py`, `task2_build_tables.py` and `task3_sweep.py` all exit 0 and regenerate their committed tables; the CLI path `etcgem transfer --experiment candida_K4_etc_area` reproduces the sweep's 1 x A* row; `python reports/candida_thermal_limit/gate_table.py` -> 79/79 PASS, exit 0, with $CANDIDAS_ROOT unset, both before and after this work (2026-09-08).

**This report was last written at** `f3e9d8f` (2026-09-08) — K4: the mechanism through the supported CLI path, and what it does to K2's requirement

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
