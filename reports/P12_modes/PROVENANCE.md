<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P12_modes/`

**STATUS: CURRENT** — The basin map of the configuration D NLDM posterior. Every number is a measurement from committed run output, and the two results that matter are robust in the way the report says: the bottleneck-barrier matrix is bimodal with 13.868 on one side and 1.785 on the other, so the count of TWO basins is unchanged for any threshold in a factor-7.8 window, and 11 of the 12 continued optimisations terminated on tolerance rather than the evaluation cap, which is what makes them local optima rather than budget artefacts. The report RETRACTS P11's reading of its seed disagreement as multimodality (the two runs' best samples are 0.266 apart, which is noise) and withdraws one sentence of OPEN_ITEMS section 0c by dated note. Two of its own intermediate readings were retracted inside it before reaching any conclusion -- the screen's 23 clusters (D5, 96 of 100 starts hit the prompt's 600-evaluation cap) and the first connectivity reduction (D6, monotone descent misread as an edge) -- and both retractions are recorded in DECISIONS.md rather than edited away. Nothing in the model, the likelihood, the weight, the floor or the tie-break was changed by this run.

*Evidence:* task0_fixed_points.py and task1_lines.py regenerate the decomposition and the four line profiles in ~2 min; addendum1_weight.py regenerates the counterfactual schemes in ~2 min; task2_basins.py regenerates the 100-start screen (~2.9 h on 16 processes, seed 21); task2b_barriers.py (~5 min) and task2c_converge.py (~1.7 h) regenerate the barrier test and the continuations; task2d_modes.py (~10 min) regenerates the mode count under D7's rule and the per-endpoint deadness and no-discount scores; task2e_minimax.py and task3_per_basin.py are arithmetic and LP reads over the committed CSVs and take under two minutes.

**This report was last written at** `4f4c56f` (2026-09-10) — P13: record -- OPEN_ITEMS, evidence, README note, four dated notes, stamps

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
