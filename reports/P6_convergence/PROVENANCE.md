<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P6_convergence/`

**STATUS: CURRENT** — a HALTED run. No fit reached a convergence target and nothing in it is quoted as a result. What it holds is diagnosis, all reproducible from the scripts beside it: the pre-flight (settings, tau, jitter), the benchmark, the E/F likelihood degeneracy and its mechanism, and why tau tracks chain length on configuration D. The three sampler-only options it added to run_gasflux_fit default to the old behaviour.

*Evidence:* preflight.py, jitter_diagnosis.py, degeneracy.py, state_vs_identifiability.py, state_detail.py, diffusion.py and bench.py regenerate their CSV/JSON outputs against the committed P4 chains (bench.py needs ~1 h of sampling; the rest are evaluations only).

**This report was last written at** `0d94f10` (2026-09-09) — P6 addendum 6: D NLDM halted; tau tracks chain length because every parameter mixes slowly, not because one direction diffuses

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
