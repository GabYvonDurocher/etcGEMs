<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P5_lb_cmax/`

**STATUS: CURRENT** — every number is produced by a script beside it against the present code. Its one emcee chain (configuration D on LB at c_max 257) is TRAPPED IN A DEAD MODE by the warm start and is kept as a record of that, not as a fit; the c_max question is answered at fixed parameter points and by local re-optimisation, deterministically. Nothing in it is a converged posterior, and it says so wherever an R2 appears.

*Evidence:* task1_parsa_lb_cmax.py, task1c_fixed_point.py, task1d_cap_sweep_fixed_point.py and task1e_reoptimise.py regenerate their CSVs (the last three read $PARSA_ROOT, read only); run_lb_fit.py regenerates the chain (~2.5 h) and lb_cmax_comparison.csv.

**This report was last written at** `6e470a3` (2026-09-09) — P5 TASK 1: c_max is the cause of the LB collapse; the LB cap is set to 450, his E/F nominal

**Committed inputs it reads (found by scanning this report's own files), and the commit each was last written at:**

| input directory | last written | date | commit subject |
|---|---|---|---|
| `strains/eciML1515/outputs/calibration_configD_LB_recipe_cmax257` | `6e470a3` | 2026-09-09 | P5 TASK 1: c_max is the cause of the LB collapse; the LB cap is set to |

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
