<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P7_walkers/`

**STATUS: CURRENT** — one bounded test -- configuration D NLDM at 128 walkers, otherwise P6's fit exactly -- read by a decision rule written before the run. The verdict (NOT MIXING: tau still tracks chain length, 26 -> 20 per 250 steps over 1500) is reproducible from chain.h5 by task3_table.py. Nothing in it is a converged posterior and nothing is quoted as one.

*Evidence:* task1_checkpoint_test{,2}.py regenerate their JSON (scratch runs, ~30 min); run_walkers.py regenerates the chain (~2.1 h); task3_table.py regenerates the checkpoint, spread and verdict tables from chain.h5.

**This report was last written at** `e6b8902` (2026-09-09) — P7 TASK 3: D NLDM at 128 walkers, 1500 steps -- tau still tracks chain length (NOT MIXING by the pre-registered rule)

**Committed inputs it reads (found by scanning this report's own files), and the commit each was last written at:**

| input directory | last written | date | commit subject |
|---|---|---|---|
| `strains/eciML1515/outputs/calibration_configD_NLDM_recipe_P7_w128` | `e6b8902` | 2026-09-09 | P7 TASK 3: D NLDM at 128 walkers, 1500 steps -- tau still tracks chain |

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
