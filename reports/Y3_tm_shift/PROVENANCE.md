<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/Y3_tm_shift/`

**STATUS: CURRENT** — a screen, not an experiment: nothing is implemented, no prior is changed and no fit is added to any strain. It reads P12's committed endpoints and runs a few hundred targeted evaluations of the likelihood already in force for eciML1515 config D on NLDM. Its decision rule was committed before any number was computed (306da82), which is checkable in the history.

*Evidence:* `python3 reports/Y3_tm_shift/task1_correlations.py` (live endpoints and correlations, ~1 min); `task2_profile.py --steps 11 --maxfev 300 --workers 8` (~19 min); `task2b_which_parameter_pays.py --maxfev 300 --workers 4` (~10 min); `task2c_meltome_bound.py --maxfev 1000 --restarts 2` (~31 min, and the second restart converges). p38's log L reproduces P12's committed -7.185975 to 1e-11.

**This report was last written at** `1571307` (2026-09-10) — Y3: TASK 3-5 -- the Li comparison, the costing, the record, and one retraction

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
