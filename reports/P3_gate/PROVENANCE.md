<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P3_gate/`

**STATUS: CURRENT** — its gate reads Parsa's committed chains and recomputes every number in it against the present code; nothing in it is carried over from an earlier state.

*Evidence:* `python reports/P3_gate/gate_def.py` regenerates gate_def_table.csv; the c_max change is reproduced by re-running gasflux_configB and gasflux_configB_cmax60.

**This report was last written at** `22ffd2e` (2026-09-13) — T2: TASK 1 -- infeasible: zero_lik and remove_inactive: [f_metab] as core options default OFF; gates OFF 79/79 60/60 byte-identical; ON for eciML1515; P3 gate table byte-identical; Parsa's theta feasible at all 12 T; status task1_done

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
