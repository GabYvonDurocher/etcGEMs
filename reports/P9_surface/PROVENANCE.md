<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P9_surface/`

**STATUS: CURRENT** — a diagnosis of the likelihood surface itself, no sampling: 22 line scans through P4's MAP with a fresh model per evaluation, the solver-tolerance and method re-scans, the per-term attribution of the largest jumps, and the chains' own acceptance statistics. Every number regenerates from the scripts beside it against the committed P4 and P7 chains.

*Evidence:* task1_scan.py (~52 min, 902 fresh builds), task2_solver.py, task2_attribute.py, task2_fva_at_jump.py and task3_chains.py regenerate their CSV/JSON/PNG outputs.

**This report was last written at** `dbb2e3d` (2026-09-09) — P9 TASK 4: ROUGH, STRUCTURAL -- the cliffs are the respiration term at one cold temperature where the LP's optimal O2 switches vertex (unique at each end, unchanged by tolerances or method); no sampler or reduction gives intervals from this likelihood; the decision is the likelihood (OPEN_ITEMS 1.15)

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
