<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P14_posterior/`

**STATUS: CURRENT** — Replaces the sampleability criterion and STOPS before the two nested runs. The criterion was written into DECISIONS.md before any of its data existed, and its headline result reverses the previous verdict on the decisive line: axis:topt_scale, which stopped P13 at 8.55 log-likelihood units, refines as 8.5517 -> 4.3325 -> 2.2295 -> 1.1236 with ratios 0.507/0.515/0.504 and is therefore pure STEEPNESS. The plateau test -- the property that actually voids nested sampling's volume shrinkage -- passes with 0 of 480 adjacent evaluations exactly equal on any of the twelve lines. Four lines nevertheless retain genuine discontinuities of 0.05-0.59 units, all one mechanism (97-100 % respiration term, growth unchanged to five decimals, O2 moving 0.57-1.86x at a single cold temperature) which is P9's LP vertex switch two orders of magnitude smaller than when it was diagnosed. The prompt stops TASK 2 on any JUMP, so the runs were not started and nothing is quoted from them; the case for lifting that conjunct is recorded in D3 and put to the PI rather than acted on, because moving a threshold after seeing its data would be the fourth time in this series. A first pass of the refinement test was WRONG and is corrected in D2 rather than edited away: it tested each line's largest step overall, which on eight of twelve lines is the feasibility transition the criterion exempts by name.

*Evidence:* task0_verify.py re-confirms state-independence after the merge in ~4 min (ten fresh builds); task1_refine.py and task1_refine2.py regenerate the refinement series in ~7 and ~20 min (fresh model per evaluation, single process); task1_plateau.py is arithmetic over P13's committed clamp scan; task1_jump_diag.py bisects and decomposes the four residual jumps in ~7 min. The seven-strain gate (79/79, 60/60) comes from the standard battery.

**This report was last written at** `84db0c5` (2026-09-10) — P14: record -- OPEN_ITEMS, evidence, README note, stamps

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
