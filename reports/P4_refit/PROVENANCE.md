<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P4_refit/`

**STATUS: CURRENT** — every number in it is produced by a script in the same directory against the present code. Its own headline is that NO chain in it converged -- nor did any of Parsa's -- so nothing in it is quoted as a converged posterior. That is a property of the sampling budget, and it is stated in the report rather than worked around.

*Evidence:* reports/P4_refit/run_fits.py regenerates the fits (~10 h) and compare.py the tables; convergence diagnostics are recorded per fit in each summary.json.

**This report was last written at** `a61a3b2` (2026-09-08) — P4 TASK 3-4: what moved, M9 first light, and the finding that governs both

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
