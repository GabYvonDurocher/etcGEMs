<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/T3_determinism/`

**STATUS: CURRENT** — Measures four candidate remedies for the R1 blocker T2 named (item 1.34) on one fixed, hashed 22-input set against a bit-reproducible fresh-process reference, and stops with a recommendation the PI must register. Only a FRESH MODEL PER EVALUATION is deterministic (0.0 deviation at every input and every evaluation), at a measured 3.2x the current cost. A solver reset per call is WORSE than doing nothing (max deviation 23.28 against 1.82, 63 % more cost): it discards the basis but not the degeneracy. Gurobi's lexicographic tie-break returns a DIFFERENT VERTEX (O2 differs by up to 4.58 on D NLDM and 8.40 on E LB) and is disqualified before any determinism verdict. Two magnitudes, plausibly one cause: D NLDM shows a 1e-8 haze with rare order-1 excursions, E LB -- an LP face -- shows 1.5-1.8 routinely. The corruption attaches to the evaluation, not to theta. The 20 highest-weight posterior samples of T2's runs 1 and 2 are clean to 2e-8, so the defect does NOT explain their disagreement and a second cause remains. Nothing implemented, enabled or run; no reserved seed consumed.

*Evidence:* t3_inputs.py builds and hashes the input set (inputs.json, sha256 8aef2dfd); t3_battery.py runs one battery per invocation (--scheme ref|A|B|C|D|Cverify, ~5-55 min each under its registered alarm); lexi_tiebreak.py is the SCRATCH lexicographic module, measurement only and not in the core; t3_table.py applies D0's decision rule to battery_*.json; t3_task2.py re-evaluates the high-weight posterior samples of T2's runs 1 and 2 in a fresh process each.

**This report was last written at** `21f8176` (2026-09-16) — T3: record -- report with the four-scheme table and the recommendation; OPEN_ITEMS 1.34 measured, 1.35 opened, 1.22 restated as a correctness question, section-4 hazard in its general form; evidence T3a-T3d; dated note in P17's report; report_status; stamps

**Committed inputs it reads (found by scanning this report's own files), and the commit each was last written at:**

| input directory | last written | date | commit subject |
|---|---|---|---|
| `strains/eciML1515/outputs/calibration_configD_NLDM_recipe_T2_validated` | `a5c3bbe` | 2026-09-15 | T2: TASK 5 -- R1 OPEN: run 3 crashed on a stored likelihood 1.28 above |

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
