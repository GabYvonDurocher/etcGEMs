<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/R3_integration/`

**STATUS: CURRENT** — The integration of P16, P17, the deck and Q1 into origin/main at 64393ed. Every number is from a command whose exit code is logged in git_commands.log. Two things are recorded against interest: PR #33's base was e2/deck rather than main and was merged without that being checked, so a fifth PR (#36) was needed to bring Q1 to main; and the curation deviates from the prompt's literal file filter, which would have admitted 2,101 MB of checkpoint blobs, in favour of the extension list that yields 15.7 MB. Neither is a scientific change: integration moved no conclusion.

*Evidence:* git_commands.log records each git invocation with its exit code; task1_hashes.txt and task1_diff_pairs.txt the SHA-256 comparison of every dirty file against P17's copy; task2_curation.json and task2_curation.txt the included/excluded file counts, sizes and the manifest verification; task5_gates_main.log the gate battery on merged main. The fresh-clone verification is reproducible by cloning origin/main and re-running the two gate scripts.

**This report is not yet committed.**

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
