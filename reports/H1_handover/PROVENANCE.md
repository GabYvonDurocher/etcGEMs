<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/H1_handover/`

**STATUS: CURRENT** — The pause-and-hand-over run: no fit, no sampler, no model or likelihood change, no derived table altered, no option enabled. Writes the calibration investigation from P16 to T3 (calibration_investigation.pdf, the successor to reports/synthesis/ for everything after 9 September), brings the deck current from the record (9 slides changed, 3 added, 59 -> 62 pages), assesses Parsa's per-medium N0 and carbon constants and finds them a LIKELIHOOD change rather than the figures update his METHODS claims -- M9's respiration observable would halve -- recommending without applying, and writes docs/HANDOVER.md for a collaborator opening the repository cold. Every number in the report is traced to a committed file and a commit.

*Evidence:* calibration_investigation.qmd renders to _output/calibration_investigation.pdf with quarto under the synthesis's house convention (header.tex, nature-communications.csl); parsa_1_9/ holds the two source files Parsa sent with their SHA-256; parsa_1_9_assessment.md verifies his arithmetic and quantifies the per-medium movement from the committed derived tables; gates_worktree.sh runs the seven-strain battery (79/79, 60/60 at main 897d78e, task0_gates.log).

**This report was last written at** `53ec4ce` (2026-09-16) — H1: repository closed -- the branch cleanup (33 deleted, 9 held by worktrees, 1 protected) and the fresh-clone proof at ee7d183 (79/79, 60/60, stamps clean, 9-page report, 62-page deck)

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
