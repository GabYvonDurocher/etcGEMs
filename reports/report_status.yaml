# Provenance status of every report directory. Read by scripts/stamp_reports.py, which writes
# reports/<name>/PROVENANCE.md.
#
# WHY THIS FILE EXISTS. Whether a report's numbers still reproduce is a CLAIM, and a claim has
# to be tested. It cannot be inferred from dates, so it is not computed: it is recorded here,
# with the evidence, and the stamp generator copies it verbatim. A report with no entry is
# stamped UNKNOWN, which is visible and honest.
#
#   status: CURRENT     the numbers reproduce from the code as it stands, and that has been checked
#           HISTORICAL  a record of an earlier model state. NOT a defect -- a working log is
#                       supposed to describe the state it was written in. The defect was only
#                       ever that it did not say so.
#           UNKNOWN     not established either way, and not claimed.
#
#   inputs: optional. The committed strain-output directories the report renders from, where an
#           audit has established them. Overrides the generator's scan, which misses paths built
#           by string join and picks up directories merely mentioned in prose.

synthesis:
  status: CURRENT
  note: >-
    every number in it is cited to a file by row id in evidence.csv, and evidence.csv looks the
    commit up from git rather than carrying a typed one. It assembles; it runs no model. Three
    claims are marked PROVISIONAL-ON-P6 and have named slots in the document's own "how to
    update" section.
  evidence: >-
    `python reports/synthesis/build_evidence.py` regenerates evidence.csv (74 rows);
    `python reports/synthesis/fig_requirement_arithmetic.py` regenerates the one figure;
    `quarto render reports/synthesis/synthesis.qmd` produces the 10-page PDF.

activation_energy:
  status: UNKNOWN
  note: >-
    not audited. Its E_a dissections and the three calibrations they rest on were written
    2026-07-10 to 2026-09-07, spanning the model changes N3 identified; whether they still
    reproduce has not been tested, and settling it means re-running the dissections.
  evidence: "no reproduction test has been run against this directory"

candida_thermal_limit:
  status: CURRENT
  note: >-
    the K1 gate reproduces the standalone Candida implementation's locked numbers today,
    and the four Candida strains' transfer outputs are byte-identical on re-run.
  evidence: >-
    `python reports/candida_thermal_limit/gate_table.py` -> 79 comparisons, 79 PASS, 0 FAIL
    with $CANDIDAS_ROOT unset (re-run at the P1 merge, 2026-09-08); `etcgem transfer
    --experiment transfer_candida` leaves a clean `git status`.

ecoli_gasflux:
  status: CURRENT
  note: >-
    every configuration A-F reproduces Parsa's numbers today. A/B/C are gated on his committed
    gas-flux CSVs; D/E/F are gated against experiment now that his respirometry has arrived.
  evidence: >-
    `python reports/P1_parsa_port/gate.py` -> 60 comparisons, 60 PASS, 0 FAIL (A/B/C);
    `python reports/P3_gate/gate_def.py` -> all ten R2 values his reports print reproduced,
    worst difference 0.009 (D/E/F).
  inputs:
    - strains/eciML1515/outputs/gasflux_configA
    - strains/eciML1515/outputs/gasflux_configB
    - strains/eciML1515/outputs/gasflux_configB_cmax60
    - strains/eciML1515/outputs/gasflux_configC
    - strains/eciML1515/outputs/gasflux_configD
    - strains/eciML1515/outputs/gasflux_configE
    - strains/eciML1515/outputs/gasflux_configF
    - strains/eciML1515/outputs/calibration_configD_NLDM_recipe_cmax120
    - strains/eciML1515/outputs/calibration_configD_LB_recipe_cmax120
    - strains/eciML1515/outputs/calibration_configD_M9_recipe_cmax120
    - strains/eciML1515/outputs/calibration_configE_NLDM_recipe_cmax120
    - strains/eciML1515/outputs/calibration_configE_LB_recipe_cmax120
    - strains/eciML1515/outputs/calibration_configE_M9_recipe_cmax120
    - strains/eciML1515/outputs/calibration_configF_NLDM_recipe_cmax120
    - strains/eciML1515/outputs/calibration_configF_LB_recipe_cmax120
    - strains/eciML1515/outputs/calibration_configF_M9_recipe_cmax120

P4_refit:
  status: CURRENT
  note: >-
    every number in it is produced by a script in the same directory against the present code.
    Its own headline is that NO chain in it converged -- nor did any of Parsa's -- so nothing
    in it is quoted as a converged posterior. That is a property of the sampling budget, and it
    is stated in the report rather than worked around.
  evidence: >-
    reports/P4_refit/run_fits.py regenerates the fits (~10 h) and compare.py the tables;
    convergence diagnostics are recorded per fit in each summary.json.

P3_gate:
  status: CURRENT
  note: >-
    its gate reads Parsa's committed chains and recomputes every number in it against the
    present code; nothing in it is carried over from an earlier state.
  evidence: >-
    `python reports/P3_gate/gate_def.py` regenerates gate_def_table.csv; the c_max change is
    reproduced by re-running gasflux_configB and gasflux_configB_cmax60.

ecoli_tpc:
  status: HISTORICAL
  note: >-
    of the eleven committed directories this report renders from, three reproduce today and
    eight do not. Two causes account for nearly all of it: a416fd1 (the sector re-grounding,
    2026-07-02) for the five 2026-07-02 directories, and 8085036 (the O2-sink closure,
    2026-07-11) for the 2026-07-09 ones. Nothing found contradicts a scientific claim -- T_opt,
    CT_max and curve shape are stable across every test -- but the report prints numbers from
    three different model states side by side.
  evidence: >-
    reports/N3_output_audit/report.md (the eleven-directory audit; arrives on main with PR #3).
    control_tuned reproduces to seven decimal places; decompose_tuned and elasticity_tuned are
    0.235% behind it, exactly the O2-sink closure's documented effect.
  inputs:                    # the authoritative list, from N3's read of assemble.py
    - strains/eciML1515/outputs/sweep_default
    - strains/eciML1515/outputs/sweep_dltkcat_ext
    - strains/eciML1515/outputs/sweep_calibrated
    - strains/eciML1515/outputs/proteome_sectors
    - strains/eciML1515/outputs/anatomy
    - strains/eciML1515/outputs/validation
    - strains/eciML1515/outputs/calibration_vanderlinden
    - strains/eciML1515/outputs/elasticity_tuned
    - strains/eciML1515/outputs/decompose_tuned
    - strains/eciML1515/outputs/control_tuned
    - strains/eciML1515/outputs/ablation_complete

N3_output_audit:
  status: HISTORICAL
  note: >-
    a working log of the output audit (2026-09-07/08), describing the repository as it then was.
    Its own findings are the reason reports/ecoli_tpc/ is stamped HISTORICAL, and its probes
    reproduce today; the audit itself is a record of that moment.
  evidence: >-
    its probes (reports/N3_output_audit/probes/) run against the present code; the audit table
    was re-derived at the P2 merge for reports/report_status.yaml's ecoli_tpc entry.

N1_overnight:
  status: HISTORICAL
  note: >-
    a working log of one night's work (2026-09-07), describing the repository as it then was.
    Its conclusions have been carried forward where they still apply; the numbers in it are of
    that state.
  evidence: "working log; not intended to track the current model state"

N2_followups:
  status: HISTORICAL
  note: >-
    a working log of the N2 follow-ups (2026-09-07). One of its actions -- regenerating
    strains/eciML1515/outputs/tpc -- was verified to have taken, and that output reproduces
    byte-identically today.
  evidence: >-
    `etcgem tpc --strain eciML1515` reproduces the committed outputs/tpc byte-identically
    (re-checked at the P1 merge, 2026-09-08).

P1_parsa_port:
  status: CURRENT
  note: >-
    its gate and its thermal-vs-carbon check both run against the present code and reproduce
    the numbers quoted in it.
  evidence: >-
    `python reports/P1_parsa_port/gate.py` -> 60/60 PASS;
    `python reports/P1_parsa_port/partF_thermal_vs_carbon.py` regenerates its own table.

P2_settle:
  status: CURRENT
  note: >-
    every number in it is produced by a script in the same directory, run against the present
    code. TASKS 1 and 2 are PROVISIONAL pending Parsa's confirmation -- that is a question of
    intent, not of reproduction.
  evidence: >-
    task1_kcat.py, task2_nldm.py and task3_cmax.py regenerate their own tables.

predictor_calibration:
  status: HISTORICAL
  note: >-
    a record of the A1 sequence-predictor calibration. It renders from its own committed
    tables and reads no strain-output directory; re-running it needs the external model
    weights and the fetched sequence data, which are gitignored.
  evidence: "self-contained; inputs are external and not committed"

K4_membrane:
  status: CURRENT
  note: >-
    every number in the report was produced by the three scripts beside it, on the code and
    the strain models as they stand in this branch, and all three re-run clean.
  evidence: >-
    `python3 reports/K4_membrane/task1_etc_complement.py`, `task2_build_tables.py` and
    `task3_sweep.py` all exit 0 and regenerate their committed tables; the CLI path
    `etcgem transfer --experiment candida_K4_etc_area` reproduces the sweep's 1 x A* row;
    `python reports/candida_thermal_limit/gate_table.py` -> 79/79 PASS, exit 0, with
    $CANDIDAS_ROOT unset, both before and after this work (2026-09-08).

K5_respire:
  status: CURRENT
  note: >-
    every number in the report was produced by the five scripts beside it, on the code and the
    strain models as they stand in this branch, and all five re-run clean.
  evidence: >-
    `task1_coupling_audit.py`, `task2_fix.py`, `task3_respiration_test.py`,
    `task3b_membrane_retest.py` and `task4_required_separation.py` all exit 0 and regenerate
    their committed tables; `etcgem transfer --experiment candida_B5_respire` exits 0;
    `python reports/candida_thermal_limit/gate_table.py` -> 79/79 PASS, exit 0, with
    $CANDIDAS_ROOT unset, both before and after this work (2026-09-08).

K6_like_for_like:
  status: CURRENT
  note: >-
    every number was produced by the three scripts beside it, on the code and the strain models
    as they stand in this branch, and all three re-run clean.
  evidence: >-
    `task1_two_measured_E.py`, `task2_like_for_like.py` and `task3_maintenance_test.py` all exit
    0 and regenerate their committed tables; `python reports/candida_thermal_limit/gate_table.py`
    -> 79/79 PASS, exit 0, with $CANDIDAS_ROOT unset (2026-09-08).

K7_envelope:
  status: CURRENT
  note: >-
    every number was produced by the three scripts beside it, on the code and the strain models
    as they stand in this branch, and all three re-run clean. No default was changed.
  evidence: >-
    `task1_convention.py`, `task2_envelope.py` and `task3_dcp_sweep.py` all exit 0 and
    regenerate their committed tables; `python reports/candida_thermal_limit/gate_table.py`
    -> 79/79 PASS, exit 0, with $CANDIDAS_ROOT unset (2026-09-08).

K8_tm_bias:
  status: CURRENT
  note: >-
    every number was produced by the three scripts beside it plus one CLI overlay run, on the
    code and strain models as they stand in this branch, and all of them re-run clean. No
    default was changed.
  evidence: >-
    `task1_premise.py`, `task2_apply_correction.py` and `task3_offset_sweep.py` all exit 0 and
    regenerate their committed tables; `etcgem transfer --experiment
    candida_B5_tm_bias_corrected` exits 0; `python reports/candida_thermal_limit/gate_table.py`
    -> 79/79 PASS, exit 0, with $CANDIDAS_ROOT unset (2026-09-08).

K9_criterion:
  status: CURRENT
  note: >-
    every number was produced by the three scripts beside it, on the code and strain models as
    they stand in this branch. No default was changed. Synechocystis' row is quoted from the
    committed ceiling table and is flagged as not reproducible from a strain build.
  evidence: >-
    `task1_is_it_arithmetic.py`, `task2_criteria.py` and `task3_common_term.py` all exit 0 and
    regenerate their committed tables; `python reports/candida_thermal_limit/gate_table.py`
    -> 79/79 PASS, exit 0, with $CANDIDAS_ROOT unset (2026-09-08).

P5_lb_cmax:
  status: CURRENT
  note: >-
    every number is produced by a script beside it against the present code. Its one emcee
    chain (configuration D on LB at c_max 257) is TRAPPED IN A DEAD MODE by the warm start and
    is kept as a record of that, not as a fit; the c_max question is answered at fixed
    parameter points and by local re-optimisation, deterministically. Nothing in it is a
    converged posterior, and it says so wherever an R2 appears.
  evidence: >-
    task1_parsa_lb_cmax.py, task1c_fixed_point.py, task1d_cap_sweep_fixed_point.py and
    task1e_reoptimise.py regenerate their CSVs (the last three read $PARSA_ROOT, read only);
    run_lb_fit.py regenerates the chain (~2.5 h) and lb_cmax_comparison.csv.

Y1_yeast_audit:
  status: CURRENT
  note: >-
    an audit of a model outside this repository. It reads a read-only clone of
    SysBioChalmers/BayesianGEM at a68307e, which is NOT committed here, so its scripts each take
    --bayesiangem <path> and none of them runs unattended. It reads no strains/ output and writes
    nothing under strains/. What it obtained and from where is in SOURCE.md.
  evidence: >-
    every number regenerates from the clone: `task_a_load.py`, `task_b_coupling_audit.py`,
    `task_b2_verifications.py`, `task_b3_free_proton_path.py`,
    `task_b4_what_etcpy_constrains.py` and `task_c_regime_test.py`, each with
    `--bayesiangem <clone>`. The audit-tool fix underneath them was proved inert on all seven
    strains before use (task0_k5_budget_recheck.csv, task0_classes_AD_recheck.json).
  inputs: []
P6_convergence:
  status: CURRENT
  note: >-
    a HALTED run. No fit reached a convergence target and nothing in it is quoted as a result.
    What it holds is diagnosis, all reproducible from the scripts beside it: the pre-flight
    (settings, tau, jitter), the benchmark, the E/F likelihood degeneracy and its mechanism, and
    why tau tracks chain length on configuration D. The three sampler-only options it added to
    run_gasflux_fit default to the old behaviour.
  evidence: >-
    preflight.py, jitter_diagnosis.py, degeneracy.py, state_vs_identifiability.py,
    state_detail.py, diffusion.py and bench.py regenerate their CSV/JSON outputs against the
    committed P4 chains (bench.py needs ~1 h of sampling; the rest are evaluations only).

Y2_regime_posterior:
  status: CURRENT
  note: >-
    Y1's regime test repeated at Li et al.'s calibrated posterior. Like Y1 it audits a model
    outside this repository, so its scripts each take --bayesiangem <clone> and --results
    <extracted Zenodo archive>; neither is committed here and none of them runs unattended. It
    reads no strains/ output and writes nothing under strains/. What was obtained and from where
    is in SOURCE.md, with checksums.
  evidence: >-
    the prior arm reproduces Y1's committed task_c_curves.csv and task_c_summary.csv
    byte-identically (git status empty after re-running Y1's own script), and reproduces them
    again through Y2's independent amortised path. The amortisation is gated against the solver's
    own measured repeatability (4.5e-10 against 3.8e-7) in task2_verify.json. The posterior file
    is checked three ways: md5 against Zenodo, the paper's per-enzyme SDs (task1_sd_crosscheck.csv)
    and Supplementary Fig. 8's nine named enzymes (task3_low_tm.csv).
  inputs: []

P7_walkers:
  status: CURRENT
  note: >-
    one bounded test -- configuration D NLDM at 128 walkers, otherwise P6's fit exactly -- read
    by a decision rule written before the run. The verdict (NOT MIXING: tau still tracks chain
    length, 26 -> 20 per 250 steps over 1500) is reproducible from chain.h5 by task3_table.py.
    Nothing in it is a converged posterior and nothing is quoted as one.
  evidence: >-
    task1_checkpoint_test{,2}.py regenerate their JSON (scratch runs, ~30 min); run_walkers.py
    regenerates the chain (~2.1 h); task3_table.py regenerates the checkpoint, spread and
    verdict tables from chain.h5.
