<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/T1_target_revision/`

**STATUS: CURRENT** — Takes the three target-revision items to their PI approval gates and stops with a decision package; runs no fit, turns on no option, implements no candidate, applies no correction. Its one hard result is the f_metab finding -- the sampled coordinate does not enter configuration D by design, shown from the growth-law branch's own comment, the configuration and the published report, and PROVEN by an 870-point invariant with maximum difference 2.02e-08 against a 1e-6 tolerance registered in advance. f_maint is a different case and stays. TASK 2 lays the observation model bare from the measurement record (replicate SDs at every temperature, no documented detection limit, non-growing series that respire) and classifies missing predictions by solver status through a three-rung retry ladder -- 876 points, 10,512 solves, 9,355 STRUCTURAL_ZERO, 0 UNRESOLVED; 765 of the 800 validated live points infeasible at every temperature (P17's 765 compatible, 81.5 % of red2's weight, as a solver fact) -- and chooses nothing. TASK 3 traces the seven curvature axes without smoothing: at D44 five SUPPORTED, two UNDETERMINED (an untraced O2 vertex change at 27 C), zero IMPLEMENTATION DEFECT; the registered clip moved twice and carried no curvature; two trace-script defects were corrected to the registration with the original outcomes retained. Two batches exceeded their registered 2 h caps and were NOT extended; pre-registered follow-on batches covered the remainder (nothing UNEVALUATED). All writes went through a worktree because the primary tree's index.lock is held stale by the sandbox VM and the task forbids deleting lock files.

*Evidence:* alarm.py self-tests the deadline; task1_invariant.py (+ --only-missing) regenerates the proof from the registered audit set (~92 min); task2_classify.py (+ --only-missing) the solver-status classification (~2-4 h in two batches); task2_datum_table.py the reconciled datum tables (minutes); task3_trace.py (+ task3_fixbase.py, task3_refine_minus.py) the seven-axis trace (~35 min) and task3_analyse.py its D7 classification; gates_worktree.sh the seven-strain battery in the worktree. Input hashes are recorded in task1_invariant.json and the trace's JSON; the red2/6800 set lives only in the archive worktree.

**This report was last written at** `6251605` (2026-09-13) — T1: curvature -- seven axes traced at D44 and five diverse points; 5 SUPPORTED, 2 UNDETERMINED (27 C O2 vertex change), 0 IMPLEMENTATION DEFECT; clip not implicated; two script corrections recorded, outcomes retained

**Committed inputs it reads (found by scanning this report's own files), and the commit each was last written at:**

| input directory | last written | date | commit subject |
|---|---|---|---|
| `strains/eciML1515/outputs/calibration_configD_NLDM_recipe_P16_reduced` | `b8b181b` | 2026-09-13 | R3: P16 post-run evidence and the 0d sequencing note, committed from t |
| `strains/eciML1515/outputs/calibration_configD_NLDM_recipe_cmax120` | `32cc234` | 2026-09-08 | P4 TASK 2: refit D_NLDM under the canonical settings |
| `strains/syn6803/outputs/tpc_syn6803_ecmodel` | `b8b181b` | 2026-09-13 | R3: P16 post-run evidence and the 0d sequencing note, committed from t |

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
