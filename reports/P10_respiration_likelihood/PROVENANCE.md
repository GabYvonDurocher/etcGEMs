<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P10_respiration_likelihood/`

**STATUS: CURRENT** — the first change to the model's likelihood in this series, made as two core options (default OFF; ON for eciML1515 in gas_exchange.yaml) and measured on the instruments that found the defect before anything sampled. No chain was run: the surface did not read SMOOTH by P9's rule. Nothing in it is a posterior.

*Evidence:* task1_check.py, task1_d3a.py [--both], task1_tol.py, lines.py (four conditions, ~35-70 min each, fresh model per evaluation), task2_floor.py, task3_table.py, task3_attribute.py, task4_regate.py and task4_candida_pfba.py regenerate every CSV/JSON beside them.

**This report was last written at** `930d038` (2026-09-09) — P10: D4 -- the TASK 3 reading rule's flat-line clause, written before the table exists

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
