<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/P13_support/`

**STATUS: CURRENT** — Replaces the respiration term's support discount with a bounded form, and STOPS before the two nested runs. Every number is a measurement from committed output. The run's two load-bearing results are negative and are reported as such: the recommended support form (impute O2 = 0 where growth stops) was WITHDRAWN because a non-growing cell still respires for maintenance -- at every dead temperature of a live endpoint the LP is feasible with O2 = 0.525-5.674 -- so imputation fails P10's own D3a state gate by 119.14 log-likelihood units; and TASK 3 found the surface NOT SAMPLEABLE at p38, with axis:topt_scale carrying 8.55 units between two FEASIBLE points against the absolute rule's 5, IDENTICALLY under the old and new support handling (8.5519 vs 8.5517), so the blocker predates this run and is untouched by it. TASK 4 and TASK 5 were therefore not attempted and nothing is quoted from them. What IS established: clamp is adopted on measurement (the mask was discarding real predictions -- 4 of 4 dead temperatures at live endpoints carry a finite O2), it is state-independent at the boundary where the current likelihood is not (0.0000 against 0.0157 over ten fresh-model evaluations), and the growth mask is an attractor for the optimiser (4 of 12 endpoints sit on it exactly). The report qualifies P11's SAMPLEABLE verdict as centre-dependent and P12's ceiling test as mask-assisted, both by dated note with no numbers edited. Two predictions were pre-registered before TASK 4 and could not be evaluated because TASK 4 did not run; they stand as written.

*Evidence:* task0_baseline.py reproduces the three baseline log-likelihoods in ~2 min; task1_schemes.py the three-scheme table and the epsilon sensitivity in ~5 min; task1b_state_and_continuity.py the D3a state test and the continuity scans in ~25 min; addendum1_why_clamp.py the feasible/infeasible classification, the knife-edge counts and the ten fresh-model evaluations in ~20 min; task3_lines.py re-scans the twelve lines at p38 under both schemes in ~16 min on 8 processes and task3_verdict.py and task3_decompose_step.py are arithmetic and a single LP pair over its output. The seven-strain gate (79/79, 60/60) and the P3 gate table are regenerated by the standard battery; the P3 table is byte-identical with the option ON.

**This report was last written at** `dea5535` (2026-09-10) — P13: gate and surface -- clamp adopted for eciML1515; TASK 3 FAILS and TASK 4 does not run

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
