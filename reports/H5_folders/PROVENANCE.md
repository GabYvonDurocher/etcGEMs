<!-- generated by scripts/stamp_reports.py -- do not edit by hand -->

# Provenance — `reports/H5_folders/`

**STATUS: CURRENT** — Filesystem and git hygiene: reduced five sibling folders to two (the repository and ../etcGEMs-venv), recovering ~3.4 GB, by removing the P17 archive worktree, the second working tree and the salvage folder. No fits, no science, no model or config change. Its one substantive result is the PROOF that the P17 archive's objects live in the primary repository's own object store and not in its checkout: the manifest sha256, its 1,905-file count and three files chosen by a rule fixed in advance (largest, smallest, report.md) were hashed in the worktree, re-derived from the branch with git show BEFORE removal, and hashed again in a recreated checkout -- all four matching at every stage, with the 104.7 MiB log materialising in full. The branch codex/p17-inactive-prior at ef1961b remains the irreplaceable part and section 4's hazard was rewritten to say so now that no checkout is on disk to make it visible.

*Evidence:* pre_removal_hashes.json records the four hashes taken before anything was removed; report.md carries the before/after folder table, the three-stage hash proof, the recreate commands and the item-by-item check of the salvage folder. Recreate the archive with 'git worktree add ../etcGEMs-p17-archive codex/p17-inactive-prior'.

**This report was last written at** `387182a` (2026-09-16) — H5: five sibling folders reduced to two -- the archive and second worktrees removed and the salvage folder deleted, recovering 3.4 GB; the archive BRANCH kept and proved recoverable by hash at three stages; the section-4 hazard rewritten to protect the branch now the checkout is gone

**Reads no committed strain-output directory** (self-contained, or its inputs are its own files).

---

*What the statuses mean.* **CURRENT** — the numbers reproduce from the code as it stands. **HISTORICAL** — a record of an earlier model state; that is what a working log is, and it is not a defect. **UNKNOWN** — not established either way. Regenerate this file with `python scripts/stamp_reports.py`; the statuses themselves live in `reports/report_status.yaml` with their evidence.
