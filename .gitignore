# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments
.venv/
**/.venv/

# OS
.DS_Store

# Editor / harness local settings
.claude/settings.local.json

# External vendored clone (has its own git history + heavy torch venv)
/DLTKcat/

# Run checkpoints
**/_checkpoint.npz

# Quarto report render output (keep .qmd, header.tex, refs, csl, assemble.py, assets)
reports/ecoli_tpc/_output/
reports/activation_energy/_output/
.quarto/
**/.quarto/
refs/

# Transient / local artifacts (FUSE temp files, editor backups, verify logs)
.fuse_hidden*
*.bak*
verify_*.log

/tpc_pipeline/
collaborator_email_draft.md
/strains/eciML1515/outputs/_archive/
# Collaborator email drafts (kept local, not version-controlled)
emails/
# Local push helper (do not track)
push_updates.sh
# Quarto / LaTeX render byproducts (keep .qmd, header.tex, assets, assemble.py)
reports/**/report.tex
reports/**/supplementary.tex
reports/**/*.aux
reports/**/*.log
reports/**/*.out
reports/**/*.toc
reports/**/*.fls
reports/**/*.fdb_latexmk
reports/**/*.synctex.gz

# tools/reconstruction: third-party code, trained weights and databases (fetch_external.sh),
# and the pipeline's own scratch work area. Same convention as /DLTKcat/.
/tools/reconstruction/external/
/tools/reconstruction/work/
/tools/reconstruction/external/predictor_venv/

# S1: the synthesis report renders here; the PDF is a build artefact like the others
reports/synthesis/_output/
