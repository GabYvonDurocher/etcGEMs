> **SUPERSEDED — 16 September 2026.** This handover is kept unedited as the record of the state at
> its date. The current handover is [HANDOVER.md](HANDOVER.md), and the investigation it summarises
> is written up in `reports/H1_handover/_output/calibration_investigation.pdf`. Two things below have since
> been overtaken: the P17 archive worktree has been relocated out of `/private/tmp` (to
> `../etcGEMs-p17-archive`), and the target revision it anticipates has been specified, approved,
> implemented, gated and run — with the result that R1 is still open for a **different** reason,
> now diagnosed. Nothing below is edited.

# Claude Code handover — 13 September 2026

**Integrate the repository FIRST. P17 is closed as a negative diagnostic result by PI decision, not by passing its original gate.** This session made no merge, push, branch switch or edit to another worktree. [INTEGRATION_STATE.md](INTEGRATION_STATE.md) gives exact inspected branch heads, cached-remote limitations, ancestry, every external uncommitted file, artifact availability, conflict rules and the proposed integration order. The completed closure is on `codex/p17-inactive-prior` in `/private/tmp/etcGEMs-p17`; identify its final commit with `git rev-parse HEAD` there. The final response supplies its full immutable hash. Do not discard the temporary worktree until merged, pushed and verified complete in a fresh clone.

## Scientific state

**P16:** Fixing dTm=0 removed one sampled direction and allowed two fifteen-dimensional runs to meet their numerical stopping condition. Their close log Z values (−26.029894 and −26.052380) did not establish reproducibility: fourteen of fifteen marginal comparisons fail and the truly inactive f_metab CDF has weighted means 0.319120 and 0.393715 instead of 0.5, with distances 0.290878 and 0.216146. P16's eigenspectrum, marginals, tail checks and completed-run audit remain preserved evidence of unreliable inference. dTm=0 assumes the meltome mean is exact; uniform melting-temperature uncertainty is excluded and would otherwise be absorbed by tm_scale and catalytic parameters. Do not fix another biological parameter to obtain apparent convergence.

**P17:** Independent reconstruction confirms the inactive-prior failure and rejects simple output arithmetic and prior-wiring explanations. Three completed conditional traces characterise ancestry concentration, stratum-dependent trapping and a roughly 60–80-fold group/pooled width mismatch. The real-path Beta control has poor coverage; tested local/global/combined/partitioned interventions do not provide a validated real correction. D44/D45 reproduce scale-dependent curvature at **ONE non-optimal point**, undermining direct transfer of the tested quadratic fit without proving all samplers fail or that the likelihood is the sole cause. The **80–87% living fractions are analytical toy outcomes from their specified priors and likelihoods**, not targets or acceptance thresholds for revised biology. The only batch running when closure arrived, D50, is retained and independently audited; its actual disposition is recorded in the final report and DECISIONS. Reserved confirmation seeds 17901–17905 are not consumed. The PI expressly changed the stopping rule to close negatively and prepare scientific-target revision; no original gate is retrospectively relaxed into a pass.

**Not established:** No validated posterior for this family. R1 remains open; R3 is provisional; R4 is untouched by P17. The emergent model's earlier a priori Van Derlinden comparison is distinct from the calibrated model, which still has no validated holdout claim. D/E/F/M9 fits and evidence-based comparisons remain blocked behind target revision and the full validation requirements. Neither matching evidence nor nuisance recovery alone clears these blockers.

## Governing discipline and next scientific step

[RIGOUR.md](RIGOUR.md) and OPEN_ITEMS §0c govern **every subsequent run**: commit preregistration before judged data; retain all outcomes; dated withdrawals, reserved seeds, independent hash audits, enforced deadlines and an unchanged target within diagnostics. Do not claim historical perfection: P17 includes plans committed after runs, D43's dispatch-order failure and D15's budget overrun.

[TARGET_REVISION_SPEC.md](TARGET_REVISION_SPEC.md) is the next scientific proposal, **awaiting PI approval**, for a separate Claude Code session. It specifies configuration-D f_metab consistency, observation-model-derived infeasibility handling and tracing the seven curvature-sensitive axes. It contains saved old/new invariants and observation arithmetic, not executed changes. Revised posterior fits launch only after PI approval and commitment of the complete quantitative validation protocol. No sampler-only follow-on is authorised under P17. The recurring P17 investigation automation was paused at the closure instruction.

## Open PI decisions and related gates

This list reconciles the historical OPEN_ITEMS wording with its dated P17 closure note; it does not reopen closed support/smoothness decisions.

- **1.27:** Approve, amend or reject the separate target-revision specification, including intentional removal of inactive f_metab versus an explicit allocation hypothesis, seven-axis mechanism tracing, and the full validation/stopping protocol. No execution yet.
- **1.25:** Choose the justified observation-model treatment of missing/infeasible respiration, distinguishing biological zero from numerical failure before seeing revised posteriors. No preferred living-fraction penalty.
- **1.15:** Remaining respiration likelihood/observation-model decision, now linked to 1.25/1.27. The historical clamp choice in closed 1.21 does not resolve missing predictions.
- **1.20:** Interpret the measured meltome low-tail tension and the uncertainty excluded by dTm=0. A mean-preserving model can grow; the earlier contrary claim was retracted. Do not infer biological identification from the reduced runs.
- **1.19:** Approve a properly preregistered independent-run confirmation programme only after the revised target is approved and ready. R1 is still open; do not simply repeat the old fits.
- **1.17:** Whether and when to commission remaining D/E/F/medium fits; blocked. Historical cost estimates are not current validation or permission.
- **1.26:** Whether to proceed to D/E/F evidence-based mechanism comparison after inference is validated; blocked. Old P16 evidence values do not yet support model selection.
- **1.14:** Whether Candida TPCs justify Li-style predictor-as-wide-prior calibration; deferred behind the E. coli recipe and provenance work.
- **1.16:** Whether Candida respiratory audits should adopt the parsimonious vertex, as a K-series biological/audit decision.
- **1.22:** Deferred lexicographic tie-break/respiratory vertex proposal (historically listed as implementation work); adoption requires a separately justified decision and is not authorised here.

**1.24 is not a fresh permission to fix another parameter:** its reduction choice was acted on in P16, but its hoped-for inference resolution failed; remaining action is transferred to 1.27. **1.23 and 1.21 remain historically closed.** External requests **1.5–1.9**, plus the formal **1.4** confirmation, remain tracked as collaborator/provenance dependencies rather than PI model choices. Q1 updates 1.9 on its own branch and must be reconciled during integration before any interpretation. OPEN_ITEMS contains all detailed histories.

## Exact standing null-test commands

The new `reports/P17_inactive_prior/null_check.py` consumes saved arrays only; it never solves or fits a model. It verifies hashes, transforms the named independent prior to its exact CDF, normalises log weights and reports weighted CDF mean/distance and weight ESS. It deliberately emits **no automatic pass and no IID p-value**. Save original run artifacts and their hashes before audit. Establish inactivity, prior independence and parameter mapping separately for every changed target; the CLI cannot prove these biological/code premises.

Run from the integrated repository root. The current interpreter is:

```sh
P17_PY='/Users/g.yvon-durocher/Library/CloudStorage/OneDrive-UniversityofExeter/Documents/work/MICROADAPT/etcGEMs-venv/bin/python'
```

Reproduce the red1 and red2 null results against their original saved hashes (choose new output names if these filenames already exist; the CLI refuses overwrite):

```sh
"$P17_PY" reports/P17_inactive_prior/null_check.py \
  --samples strains/eciML1515/outputs/calibration_configD_NLDM_recipe_P16_reduced/samples_red1.npy \
  --logwt strains/eciML1515/outputs/calibration_configD_NLDM_recipe_P16_reduced/logwt_red1.npy \
  --column 7 --prior p16-f-metab \
  --expected-samples-sha256 fb134be7f90066e7129bdc0acba3b1f89227a49fc398921912b7b88bce7e1cbf \
  --expected-logwt-sha256 35b32f1b9ba6d21d156101e437cec155f99c8ac6f350229548a6259856af4ac2 \
  --output reports/P17_inactive_prior/handover_null_red1.json

"$P17_PY" reports/P17_inactive_prior/null_check.py \
  --samples strains/eciML1515/outputs/calibration_configD_NLDM_recipe_P16_reduced/samples_red2.npy \
  --logwt strains/eciML1515/outputs/calibration_configD_NLDM_recipe_P16_reduced/logwt_red2.npy \
  --column 7 --prior p16-f-metab \
  --expected-samples-sha256 868541d450e0cca1517e0df880af10ccbe5fb62e7ecc4240c3db1ee73b66ee16 \
  --expected-logwt-sha256 e59e07175138fb759dafbe6345832d9a0ec7db2df7980af7344a941faca7f99f \
  --output reports/P17_inactive_prior/handover_null_red2.json
```

Expected CDF mean/distance: red1 **0.3191196133 / 0.2908781416**; red2 **0.3937148977 / 0.2161460455**. The closure reruns saved in `closure_null_red1.json` and `closure_null_red2.json` reproduce the independent historical audit to 1e−12. Weight ESS values (~5988/~3252) are not independent sample counts.

For **any future run**, substitute its actual paths, independently saved hashes and verified zero-based column. Samples must be a two-dimensional NumPy `.npy` array in the coordinate named by `--prior`, with one corresponding unnormalised nested log weight per row. Include final live points exactly once. Do not feed unit-cube f values to `p16-f-metab`, or log(b) to `beta31`:

```sh
"$P17_PY" reports/P17_inactive_prior/null_check.py \
  --samples "$FUTURE_SAMPLES_NPY" --logwt "$FUTURE_LOGWT_NPY" \
  --column "$INACTIVE_COLUMN" --prior p16-f-metab \
  --expected-samples-sha256 "$RECORDED_SAMPLES_SHA256" \
  --expected-logwt-sha256 "$RECORDED_LOGWT_SHA256" \
  --output "$NEW_NULL_AUDIT_JSON"

"$P17_PY" reports/P17_inactive_prior/null_check.py \
  --samples "$FUTURE_BETA_SAMPLES_NPY" --logwt "$FUTURE_BETA_LOGWT_NPY" \
  --column "$BETA_COLUMN" --prior beta31 \
  --expected-samples-sha256 "$RECORDED_BETA_SAMPLES_SHA256" \
  --expected-logwt-sha256 "$RECORDED_BETA_LOGWT_SHA256" \
  --output "$NEW_BETA_AUDIT_JSON"
```

These future variables are required run-specific inputs, not defaults. If f_metab is intentionally removed, use the separately registered independent diagnostic coordinate and `--prior uniform` for its physical Uniform(0,1) value; retain `beta31` for b with exact CDF b³. The wrong-Uniform Beta contrast uses the same Beta arrays with `--prior uniform` and a separate output filename. Changing priors requires an explicitly implemented, independently checked CDF rather than reusing an incorrect option.

For complete historical reconstruction inspect `independent_audit.py`, `input_manifest.json` and P16's saved checkpoint audits. Do not rerun scripts that overwrite historical JSON simply to obtain the standing null statistic. P17's final D50 raw-array audit is `audit_calibration_closure.py`; its saved output is `stratified_calibration/closure_audit.json`. Every future certification must add active-distribution, region-occupancy and feasibility-aware predictive checks under the approved preregistration. Null recovery and evidence agreement alone remain insufficient.


---

## Integration completed — 2026-09-13 (R3)

_Appended by R3; the original handover above is unaltered._

`origin/main` is **`64393edede223feebbcebbf4554d548f38e03cf9`**. P16, P17 (curated), the E2–E6 deck
and Q1 are all merged. Both gates pass on main and from a fresh clone (79/79, 60/60); the deck is
59 pages; 549 P17 files in a fresh clone match `closure_manifest.json` with none mismatched.

**What changed against this document's expectations, and what did not.**

- **P17 is curated on main, complete on a local branch.** The full 2,962 MB set could not be pushed
  (`unif_benchmark.log` = 109,809,818 bytes > 100 MiB, no LFS). 580 files / 15.7 MB are on main;
  the rest stays on `codex/p17-inactive-prior` at `ef1961b`, never pushed, with every hash in the
  unchanged manifest. `reports/P17_inactive_prior/ARCHIVE.md` names each excluded file over 5 MB.
- **Commit hashes cited in DECISIONS, CLOSURE_VERIFICATION and INTEGRATION_STATE refer to that local
  branch and remain valid there.** They do not resolve in a clone of `origin/main`, by design.
- **The E6-vs-E5 duplication concern was unfounded** — inspected slide by slide; E6 revised rather
  than re-applied.
- **`/private/tmp/etcGEMs-p17` must be kept**, but the evidence is not at risk: its `.git` is a
  pointer and all blobs live in the primary repository's object store. Relocating it is item 1.28.

**Unchanged:** the scientific state above, `RIGOUR.md`'s governing discipline, and
`TARGET_REVISION_SPEC.md` as the next step awaiting PI approval. Integration moved no scientific
conclusion.
